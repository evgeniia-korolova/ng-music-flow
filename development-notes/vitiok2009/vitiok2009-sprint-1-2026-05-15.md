Sprint 1: Components, FSD & Basic Syntax — 2026-05-15
What was done:

Familiarized myself with the Feature-Sliced Design (FSD) architecture.

Completed my first task on the Git board.

Created the `Artist` interface and mock data for display.

Developed the `artist-card` component, which receives data via `input()`.

Displayed all cards in the `artists-list` component using the new `@for` control flow.

Figured out lazy loading using `@defer` and learned how to display a fake card (skeleton) using `@placeholder` while the data is loading.

Got my first hands-on experience with Tailwind CSS for styling.

Reviewed all core Week 1 topics: Components, Data Binding, Input/Output, Services, and Lifecycle Hooks.

Problems:

The FSD architecture was very difficult to grasp right away due to its complex and strict folder structure.

I kept forgetting that when outputting signal values in the HTML template, I need to add parentheses `()` since they are functions. Because of this, the data simply didn't render.

Solutions:

For FSD, I spent extra time analyzing the project structure and documentation until the logic of separating features and entities finally clicked.

For the signals issue, I debugged the template, realized my mistake with the missing (), and now I've built a solid habit of calling them properly.

Plans:
Next week, I plan to build the Details page and dive deep into routing. I also want to implement the `output()` function in practice, as there was no suitable use case for it in this week's tasks.

Time spent: ~12 hours.

Note: The core thoughts, reflections, and content of this entry were written by me personally. AI (Gemini) was used only to help structure and translate the text into proper English.
